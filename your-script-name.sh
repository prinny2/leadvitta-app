#!/usr/bin/env bash
# Vercel Ignored Build Step semantics:
# exit 0 skips the deployment; exit 1 lets the build continue.
# This file keeps builds working while the project setting points here.
exit 1
